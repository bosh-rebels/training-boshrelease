---
name: nginx

templates:
  bin/ctl.sh: bin/ctl
  etc/nginx.conf.erb: etc/nginx.conf

packages:
  - nginx

properties:
  port:
    description: This is the port where the nginx server listens
    default: 80
